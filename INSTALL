maiddown.py downloads the MAID2007 cross-sections from their website.

datatable.py pre-calculates all form factors and cross-sections which will be
used in the package.

To use the package, make a symbolic link of the package in your working
directory, copy the maiddown.py and datatable.py to the same directory and
execute them in order.
